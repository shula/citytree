from django.conf.urls.defaults import *
from django.conf import settings

urlpatterns = patterns('citytree.citycomments',
        # unused - try to reuse the normal post target
#            url(r'^post/$',          'citycomments.post_comment',       name='citycomments-post-comment'),
)

